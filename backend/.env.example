GEMINI_API_KEY=AIza...
PORT=3001

# --- Solo para pruebas del respaldo con Ollama, no dejar en true en la demo ---
# FORCE_GEMINI_FAILURE=true    # simula un 503 de Gemini y fuerza el fallback a Ollama
# FORCE_OLLAMA_FAILURE=true    # (combinado con la anterior) simula que Ollama también falla
